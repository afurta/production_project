describe('AuthSlice', () => {
  it('Check username', () => {
    expect(1).toBe(1)
    // const state: DeepPartial<AuthSchema> = { username: '123'}

    // expect(AuthReducer(state as AuthSchema, AuthActions.setUsername('1234'))).toStrictEqual({username: '1234'})
  })

  it('Check password', () => {
    expect(1).toBe(1)
    // const state: DeepPartial<AuthSchema> = { password: '123'}

    // expect(AuthReducer(state as AuthSchema, AuthActions.setPassword('1234'))).toStrictEqual({password: '1234'})
  })
})
export {}
